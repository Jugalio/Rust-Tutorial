{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceRoot}/target/debug/guessing_game.exe",
            "args": [],
            "cwd": "${workspaceRoot}",
            "sourceLanguages": ["rust"],
            "terminal": "integrated"
        }
    ]
}
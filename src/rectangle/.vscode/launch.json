{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceRoot}/target/debug/rectangle.exe",
            "args": [],
            "cwd": "${workspaceRoot}",
            "sourceLanguages": ["rust"],
            "terminal": "integrated"
        }
    ]
}